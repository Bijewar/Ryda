'use client';

import * as React from 'react';

/**
 * ThemeToggle — Ryda is locked to pure premium light mode.
 * Retained as a no-op / null component for backward compatibility.
 */
export interface ThemeToggleProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {}

export function ThemeToggle(_props: ThemeToggleProps): React.ReactElement | null {
  return null;
}
